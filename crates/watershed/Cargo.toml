[package]
name = "watershed"
description = "Terrain field stacks, a whole-grid water solver, and a terrain file format."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
glam.workspace = true
ron.workspace = true
serde.workspace = true
thiserror.workspace = true
zstd.workspace = true

# TODO(jb-comment): why the bake's parallelism is a target-gated dependency rather than a
# cargo feature, and what the single-threaded wasm path costs.
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
rayon.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
